name: Boolean test

options:
    admin:
        icon: check_box
        list_display:
        - id
        - checkbox

fields:
    checkbox:
        name: 'Is checked?'
        type: boolean

    checkbox_optional:
        name: 'Is checked? (optional)'
        type: boolean
        required: false
